# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Locally-installed agent skills (npx skills add) — third-party, not part
# of the EZJOB app itself; keep them usable locally without committing
# them into project history.
.agents/
.claude/skills/
skills-lock.json

# Generated i18n translation exports (working files, regenerate via npm run i18n:export)
i18n-exports/

# Local scratch/reference files (e.g. content-brief drafts) — not app source
scratch/
